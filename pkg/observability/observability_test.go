package observability

import "testing"

// TestObservabilityCompiles is a placeholder smoke test that validates
// the package compiles. The middleware tests (httptest + zaptest) and
// the OTel exporter tests live alongside the real wiring in Plan 02
// Task 2.
func TestObservabilityCompiles(t *testing.T) {
	t.Parallel()
}

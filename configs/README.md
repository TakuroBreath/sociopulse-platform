# configs/

YAML configuration files. Layered: defaults committed here; environment
overrides via env vars; secrets via External Secrets Operator (Plan 01).

## configs/development/

Local-development defaults. Plan 02 fills config.yaml.

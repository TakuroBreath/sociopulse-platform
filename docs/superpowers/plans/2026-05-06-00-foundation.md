# Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bootstrap the СоциоПульс monorepo with Go module, directory structure, linters, Makefile, Dockerfile, CI, doc tree, and a runnable "hello world" `cmd/api` with `/healthz` so subsequent plans can build on a known-good green-field project.

**Architecture:** Standard Go project layout, single Go module at root, multi-binary cmd/-tree (`cmd/api`, `cmd/worker`, `cmd/migrator`, `cmd/telephony-bridge`, `cmd/recording-uploader`). Internal modules under `internal/<module>/{api,service,store,events}`. Strict module boundary enforcement via `depguard` linter. Frontend lives in `web/` (set up in Plan 15). Documentation under `docs/{architecture,runbooks,specs,plans,api}`.

**Tech Stack:** Go 1.22+, Make, Docker (multi-stage), golangci-lint v1.59+, GitHub Actions, pre-commit, MIT/proprietary LICENSE.

**Spec sections covered:** §4.2 (deploy artifacts), §5.1–5.5 (module decomposition, layout principles), §17 (test strategy baseline).

---

## File Structure

This plan creates the following file tree. Each file's purpose is one-line documented; subsequent plans add real content.

```
sociopulse/                                    # repo root, current working dir
├── .gitignore                                 # ignore build artifacts, secrets, IDE files
├── .editorconfig                              # consistent indent/EOL across editors
├── .golangci.yml                              # linter config with depguard rules
├── .pre-commit-config.yaml                    # pre-commit hooks
├── Makefile                                   # build/test/lint/run targets
├── Dockerfile                                 # multi-stage Go build for cmd/api
├── go.mod                                     # Go module manifest
├── go.sum                                     # Go module checksums (autogen)
├── LICENSE                                    # proprietary license placeholder
├── README.md                                  # top-level project intro + quickstart
├── ARCHITECTURE.md                            # short pointer to docs/superpowers/specs/
├── CONTRIBUTING.md                            # how to develop, branch, test, commit
├── CODEOWNERS                                 # ownership for review routing
│
├── cmd/                                       # executable entry points
│   ├── api/
│   │   ├── main.go                            # bootstrap cmd/api, /healthz handler
│   │   └── main_test.go                       # smoke test for /healthz
│   ├── worker/
│   │   └── README.md                          # placeholder, fleshed out in later plan
│   ├── migrator/
│   │   └── README.md                          # placeholder
│   ├── telephony-bridge/
│   │   └── README.md                          # placeholder
│   └── recording-uploader/
│       └── README.md                          # placeholder
│
├── internal/                                  # private packages, importable only by this module
│   ├── auth/api/.gitkeep
│   ├── crm/api/.gitkeep
│   ├── surveys/api/.gitkeep
│   ├── dialer/api/.gitkeep
│   ├── realtime/api/.gitkeep
│   ├── recording/api/.gitkeep
│   ├── analytics/api/.gitkeep
│   ├── reports/api/.gitkeep
│   ├── billing/api/.gitkeep
│   ├── audit/api/.gitkeep
│   ├── tenancy/api/.gitkeep
│   ├── gateway/api/.gitkeep
│   └── README.md                              # documents module boundary rules
│
├── pkg/                                       # potentially-shareable utility packages
│   └── README.md                              # placeholder
│
├── migrations/                                # SQL migrations (added in Plan 03)
│   └── .gitkeep
│
├── configs/                                   # YAML configs
│   ├── development/
│   │   └── config.yaml                        # dev defaults
│   └── README.md                              # explains config layering
│
├── deployments/                               # IaC + k8s (filled in Plan 01)
│   └── README.md                              # placeholder
│
├── web/                                       # React frontend (filled in Plan 15)
│   └── README.md                              # placeholder
│
├── tests/                                     # cross-package E2E + load tests
│   ├── e2e/.gitkeep
│   └── load/.gitkeep
│
├── docs/
│   ├── superpowers/                           # already exists with specs/, plans/
│   ├── architecture/                          # ADR copies + diagrams
│   │   └── README.md
│   ├── runbooks/                              # ops procedures
│   │   └── README.md
│   └── api/                                   # OpenAPI/Protobuf
│       └── README.md
│
└── .github/workflows/
    ├── ci.yml                                 # lint + test + build on every PR
    └── codeql.yml                             # security scanning
```

---

## Task 1: Initialize git repository

**Files:**
- Create: `.gitignore`
- Modify: (init `.git/`)

- [ ] **Step 1: Verify working directory**

Run from repo root: `pwd`
Expected: `/Users/user/call-center/social-pulse`

If output differs, `cd` to the correct path. All subsequent commands assume this is the working directory.

- [ ] **Step 2: Initialize git**

Run: `git init -b main`
Expected: `Initialized empty Git repository in .../sociopulse/.git/`

- [ ] **Step 3: Write `.gitignore`**

Create `.gitignore` with this content:

```gitignore
# Go build artifacts
/bin/
/dist/
*.exe
*.test
*.out
coverage.txt
coverage.html

# Go module cache (when vendored locally)
/vendor/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.crt

# Frontend
/web/node_modules/
/web/dist/
/web/.vite/
/web/coverage/

# Local dev databases / volumes
/data/
/var/

# Generated docs/openapi
/docs/api/generated/

# tmp / scratch
/tmp/
*.bak
```

- [ ] **Step 4: Verify gitignore is in place**

Run: `cat .gitignore | head -5`
Expected: shows the first 5 lines starting with `# Go build artifacts`.

- [ ] **Step 5: Commit**

```bash
git add .gitignore
git commit -m "chore: init git repo with .gitignore"
```

Expected: `1 file changed, ~30 insertions(+)`.

---

## Task 2: Add `.editorconfig`

**Files:**
- Create: `.editorconfig`

- [ ] **Step 1: Write `.editorconfig`**

```editorconfig
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.go]
indent_style = tab

[Makefile]
indent_style = tab

[*.md]
trim_trailing_whitespace = false

[*.{yml,yaml}]
indent_size = 2

[*.{js,ts,tsx,jsx,json,html,css}]
indent_size = 2
```

- [ ] **Step 2: Commit**

```bash
git add .editorconfig
git commit -m "chore: add .editorconfig"
```

---

## Task 3: Initialize Go module

**Files:**
- Create: `go.mod`

- [ ] **Step 1: Init Go module**

Run: `go mod init github.com/sociopulse/platform`
Expected: `go: creating new go.mod: module github.com/sociopulse/platform`

If you have a different organization GitHub path, replace `sociopulse/platform` everywhere in this plan with your actual path.

- [ ] **Step 2: Verify Go version**

Run: `go version`
Expected: `go version go1.22.x` or higher. If lower, install Go 1.22+ before continuing.

- [ ] **Step 3: Pin Go version in go.mod**

Open `go.mod`. It should look like:
```
module github.com/sociopulse/platform

go 1.22
```

Edit if needed to set `go 1.22` (or higher matching your installed version).

- [ ] **Step 4: Verify module compiles (no code yet)**

Run: `go build ./...`
Expected: no output (success). If error "no Go files in ...", that's fine — we have no Go files yet.

Use: `go vet ./...`
Expected: no output.

- [ ] **Step 5: Commit**

```bash
git add go.mod
git commit -m "chore: init go module github.com/sociopulse/platform"
```

---

## Task 4: Create directory skeleton with `.gitkeep`

**Files:**
- Create: 12 `internal/<module>/api/.gitkeep` files
- Create: `migrations/.gitkeep`, `tests/e2e/.gitkeep`, `tests/load/.gitkeep`
- Create: README placeholders in `cmd/`, `internal/`, `pkg/`, `configs/`, `deployments/`, `web/`, `docs/architecture/`, `docs/runbooks/`, `docs/api/`

- [ ] **Step 1: Create internal-module directories**

```bash
for mod in auth crm surveys dialer realtime recording analytics reports billing audit tenancy gateway; do
  mkdir -p "internal/$mod/api"
  touch "internal/$mod/api/.gitkeep"
done
```

Verify: `ls internal/`
Expected: lists all 12 module directories.

- [ ] **Step 2: Create cmd-binary directories**

```bash
mkdir -p cmd/api cmd/worker cmd/migrator cmd/telephony-bridge cmd/recording-uploader
```

- [ ] **Step 3: Create supporting directories**

```bash
mkdir -p migrations configs/development deployments web tests/e2e tests/load
mkdir -p docs/architecture docs/runbooks docs/api
touch migrations/.gitkeep tests/e2e/.gitkeep tests/load/.gitkeep
```

- [ ] **Step 4: Add internal/README.md (module-boundary rules)**

Create `internal/README.md`:

```markdown
# Internal modules

Each subdirectory here is a domain module of the СоциоПульс monolith.

## Structure

Every module has these subpackages:

- `api/` — public Go interfaces and DTO types. **Other modules import only from here.**
- `service/` — business logic. Internal to the module. Not importable by other modules.
- `store/` — Postgres access. Internal.
- `events/` — NATS publishers/subscribers. Internal.

## Boundary rule

Cross-module Go imports allowed only between `<module>/api` packages.

The `depguard` rule in `.golangci.yml` enforces this: importing `internal/dialer/service`
from `internal/crm/...` will fail lint.

## Module catalog

See `docs/superpowers/specs/2026-05-06-sociopulse-system-design.md` §5.2 for the full
list and responsibilities.
```

- [ ] **Step 5: Add placeholder READMEs**

Use a small loop to write identical placeholders quickly:

```bash
cat > cmd/worker/README.md <<'EOF'
# cmd/worker

Background worker binary (scheduled jobs, async tasks via asynq, retention, retries).
Implementation in subsequent plans (see Plan 13 for analytics, Plan 12 for retention).
EOF

cat > cmd/migrator/README.md <<'EOF'
# cmd/migrator

Database migration runner (golang-migrate). Run as a Kubernetes Job pre-deploy.
Implementation in Plan 03 (Database & migrations).
EOF

cat > cmd/telephony-bridge/README.md <<'EOF'
# cmd/telephony-bridge

ESL bridge between cmd/api and FreeSWITCH cluster.
Implementation in Plan 09.
EOF

cat > cmd/recording-uploader/README.md <<'EOF'
# cmd/recording-uploader

Sidecar daemon running on each FreeSWITCH VM to encrypt and upload call recordings.
Implementation in Plan 12.
EOF

cat > pkg/README.md <<'EOF'
# pkg

Shareable utility packages — anything we'd consider open-sourcing or extracting.
Currently empty.
EOF

cat > configs/README.md <<'EOF'
# configs

YAML configuration layered as: defaults -> environment-specific -> environment variables.

- `development/config.yaml` — local-dev defaults
- `staging/config.yaml` — staging cluster (added in Plan 01)
- `production/config.yaml` — production cluster (added in Plan 01)

See spec §14 for full structure.
EOF

cat > deployments/README.md <<'EOF'
# deployments

Infrastructure-as-Code and Kubernetes manifests.
Implementation in Plan 01 (Infrastructure).
EOF

cat > web/README.md <<'EOF'
# web

React + TypeScript + Vite frontend.
Implementation in Plan 15 (Frontend foundation) and onwards.
EOF

cat > docs/architecture/README.md <<'EOF'
# Architecture documentation

ADR copies extracted from the system design doc, supplementary diagrams, and
component-specific architecture notes.

The authoritative spec lives in `docs/superpowers/specs/2026-05-06-sociopulse-system-design.md`.
EOF

cat > docs/runbooks/README.md <<'EOF'
# Runbooks

Operational procedures: deployment, incident response, disaster recovery, oncall guides.
Populated as we build.
EOF

cat > docs/api/README.md <<'EOF'
# API documentation

OpenAPI specs for HTTP endpoints, Protobuf for gRPC services.
Generated portions land under `generated/` (gitignored).
EOF
```

- [ ] **Step 6: Verify tree structure**

Run: `find . -type d -not -path './.git*' -not -path './docs/superpowers*' -not -path './social-pulse-maket*' | sort | head -40`
Expected: lists `cmd/api`, `cmd/migrator`, `cmd/worker`, `cmd/telephony-bridge`, `cmd/recording-uploader`, `configs/development`, `deployments`, `docs/api`, `docs/architecture`, `docs/runbooks`, `internal/<each-module>/api`, `migrations`, `pkg`, `tests/e2e`, `tests/load`, `web`.

- [ ] **Step 7: Commit**

```bash
git add cmd/ internal/ pkg/ configs/ deployments/ web/ tests/ migrations/ docs/architecture/ docs/runbooks/ docs/api/
git commit -m "chore: scaffold directory structure with placeholders"
```

---

## Task 5: Add LICENSE, README, CONTRIBUTING, CODEOWNERS

**Files:**
- Create: `LICENSE`
- Create: `README.md`
- Create: `ARCHITECTURE.md`
- Create: `CONTRIBUTING.md`
- Create: `CODEOWNERS`

- [ ] **Step 1: Add LICENSE (proprietary placeholder)**

Create `LICENSE`:

```
Copyright (c) 2026 СоциоПульс. All rights reserved.

This source code is proprietary and confidential. Unauthorized copying,
transfer, redistribution, or modification of this file or any part of this
project, via any medium, is strictly prohibited without prior written consent
from the copyright holder.

For licensing inquiries: legal@sociopulse.ru
```

If the project is to be open-sourced under a permissive license, replace this with the chosen license text (e.g., MIT, Apache-2.0).

- [ ] **Step 2: Add `README.md`**

```markdown
# СоциоПульс

SaaS-платформа для проведения телефонных социологических опросов: автодозвон,
анкетирование, контроль операторов, контроль качества записей.

## Documentation

- **System design:** [docs/superpowers/specs/2026-05-06-sociopulse-system-design.md](docs/superpowers/specs/2026-05-06-sociopulse-system-design.md)
- **Architecture overview:** [ARCHITECTURE.md](ARCHITECTURE.md)
- **Contributing:** [CONTRIBUTING.md](CONTRIBUTING.md)
- **Implementation plans:** [docs/superpowers/plans/](docs/superpowers/plans/)

## Quickstart

Requirements: Go 1.22+, Docker, Make.

```bash
# Run linters
make lint

# Run tests
make test

# Build all binaries
make build

# Run cmd/api locally on :8080
make run
```

Visit http://localhost:8080/healthz to verify.

## Repository layout

- `cmd/` — executable entry points (api, worker, migrator, telephony-bridge, recording-uploader)
- `internal/` — private domain modules (auth, crm, surveys, dialer, realtime, recording, ...)
- `pkg/` — shareable utility packages
- `migrations/` — SQL migrations
- `configs/` — YAML configs (per environment)
- `deployments/` — IaC + Kubernetes manifests
- `web/` — React frontend
- `docs/` — documentation tree

## License

Proprietary. See [LICENSE](LICENSE).
```

- [ ] **Step 3: Add `ARCHITECTURE.md`**

```markdown
# Architecture

This is a short pointer file. The authoritative system design lives in:

**[docs/superpowers/specs/2026-05-06-sociopulse-system-design.md](docs/superpowers/specs/2026-05-06-sociopulse-system-design.md)**

## TL;DR

- **Style:** Modular monolith on Go + 2 sidecars (`telephony-bridge`, `recording-uploader`) + FreeSWITCH cluster.
- **Cloud:** Yandex Cloud (152-ФЗ residency).
- **Telephony:** Self-hosted FreeSWITCH with multi-trunk routing.
- **Operator audio:** WebRTC via mod_verto.
- **Auto-dialer model:** Progressive (1:1) for v1.
- **Storage:** PostgreSQL (OLTP, RLS multi-tenant), ClickHouse (OLAP), Redis 7 (FSM/queues), Yandex Object Storage (recordings + reports), Yandex KMS (per-tenant KEK).
- **Event bus:** NATS JetStream.
- **Frontend:** React 18 + TypeScript + Vite, sourced from prototype `SocioPulse.html` and `social-pulse-maket/project/`.

## ADRs

Numbered ADRs (001–013) live inside the system design doc, §22.
```

- [ ] **Step 4: Add `CONTRIBUTING.md`**

```markdown
# Contributing

## Development workflow

1. Branch from `main`: `git checkout -b feat/<short-description>` (or `fix/`, `chore/`, `refactor/`, `docs/`).
2. Write tests first (TDD). All new code must have tests.
3. Run `make lint test` before pushing.
4. Open a PR. CI must pass. Get one approving review.
5. Squash-merge to `main`.

## Commit messages

Follow [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` new feature
- `fix:` bug fix
- `chore:` tooling, config, no code change
- `refactor:` code change without behavior change
- `test:` test-only changes
- `docs:` documentation only
- `perf:` performance improvement
- `build:` build/CI change

Scope optional but encouraged: `feat(dialer): add progressive ratio support`.

## Code style

- Go: `gofmt`, `goimports`, `golangci-lint` enforced via `.golangci.yml`. Tab indents.
- TypeScript: `eslint`, `prettier`. 2-space indents.
- Sql: lower-case keywords, snake_case identifiers.
- Yaml: 2-space indents, strings double-quoted only when ambiguous.

## Module boundaries

Cross-module Go imports allowed **only** between `internal/<module>/api` packages.
Importing another module's `service`, `store`, or `events` is a lint error.
See [internal/README.md](internal/README.md).

## Testing

- **Unit tests:** Go `testify`, TS `vitest`. Coverage ≥ 70% on business logic, ≥ 90% for `dialer.FSM`, `surveys.Runtime`, `RDDGenerator`, `Router`.
- **Integration tests:** `testcontainers-go` against ephemeral Postgres/Redis/NATS.
- **E2E tests:** Playwright in `tests/e2e/`.

## Secrets

Never commit:
- `.env` files (only `.env.example` is allowed)
- `*.pem`, `*.key`, `*.crt`
- Hardcoded passwords/tokens

CI runs `gitleaks` to catch leaked secrets.

## Design changes

Significant architectural changes require an ADR. Add it to the system design
doc (§22) or as a standalone file under `docs/architecture/adr-NNN-<topic>.md`.
```

- [ ] **Step 5: Add `CODEOWNERS`**

Create `CODEOWNERS` (this is `.github/CODEOWNERS` if hosted on GitHub; here at root for general visibility):

```
# Default owners
* @sociopulse/core-team

# Specific module ownership (fill in as team grows)
/internal/dialer/        @sociopulse/telephony-team
/internal/recording/     @sociopulse/telephony-team
/cmd/telephony-bridge/   @sociopulse/telephony-team
/cmd/recording-uploader/ @sociopulse/telephony-team
/web/                    @sociopulse/frontend-team
/deployments/            @sociopulse/devops-team
```

If team groups don't exist yet, leave as-is — they'll be created when the first hires happen. The file at least documents the intended ownership.

- [ ] **Step 6: Commit**

```bash
git add LICENSE README.md ARCHITECTURE.md CONTRIBUTING.md CODEOWNERS
git commit -m "docs: add LICENSE, README, ARCHITECTURE, CONTRIBUTING, CODEOWNERS"
```

---

## Task 6: Write minimal `cmd/api` with `/healthz`

**Files:**
- Create: `cmd/api/main.go`
- Create: `cmd/api/main_test.go`

- [ ] **Step 1: Write the failing test**

Create `cmd/api/main_test.go`:

```go
package main

import (
	"net/http"
	"net/http/httptest"
	"testing"
)

func TestHealthzReturnsOK(t *testing.T) {
	t.Parallel()

	req := httptest.NewRequest(http.MethodGet, "/healthz", nil)
	rec := httptest.NewRecorder()

	healthzHandler(rec, req)

	if got, want := rec.Code, http.StatusOK; got != want {
		t.Fatalf("status: got %d, want %d", got, want)
	}
	if got, want := rec.Body.String(), "ok\n"; got != want {
		t.Fatalf("body: got %q, want %q", got, want)
	}
}

func TestHealthzReturnsContentTypePlain(t *testing.T) {
	t.Parallel()

	req := httptest.NewRequest(http.MethodGet, "/healthz", nil)
	rec := httptest.NewRecorder()

	healthzHandler(rec, req)

	if got, want := rec.Header().Get("Content-Type"), "text/plain; charset=utf-8"; got != want {
		t.Fatalf("Content-Type: got %q, want %q", got, want)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./cmd/api/...`
Expected: build error — `undefined: healthzHandler`. This is the expected failing state.

- [ ] **Step 3: Write the minimal implementation**

Create `cmd/api/main.go`:

```go
// Package main is the entrypoint for cmd/api — the СоциоПульс monolith HTTP/WS server.
//
// Subsequent plans add: config loading, observability (zap, OTel, Prometheus),
// gateway middleware, module registration, WS hub, gRPC for sidecars.
package main

import (
	"context"
	"errors"
	"fmt"
	"log"
	"net/http"
	"os"
	"os/signal"
	"syscall"
	"time"
)

const (
	defaultAddr            = ":8080"
	defaultReadTimeout     = 10 * time.Second
	defaultWriteTimeout    = 30 * time.Second
	defaultShutdownTimeout = 15 * time.Second
)

func main() {
	if err := run(); err != nil {
		log.Fatalf("cmd/api: %v", err)
	}
}

func run() error {
	addr := os.Getenv("HTTP_ADDR")
	if addr == "" {
		addr = defaultAddr
	}

	mux := http.NewServeMux()
	mux.HandleFunc("/healthz", healthzHandler)

	srv := &http.Server{
		Addr:         addr,
		Handler:      mux,
		ReadTimeout:  defaultReadTimeout,
		WriteTimeout: defaultWriteTimeout,
	}

	ctx, stop := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
	defer stop()

	errCh := make(chan error, 1)
	go func() {
		log.Printf("cmd/api: listening on %s", addr)
		if err := srv.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
			errCh <- err
		}
	}()

	select {
	case <-ctx.Done():
		log.Printf("cmd/api: shutdown signal received")
	case err := <-errCh:
		return fmt.Errorf("listen: %w", err)
	}

	shutdownCtx, cancel := context.WithTimeout(context.Background(), defaultShutdownTimeout)
	defer cancel()
	if err := srv.Shutdown(shutdownCtx); err != nil {
		return fmt.Errorf("shutdown: %w", err)
	}
	log.Printf("cmd/api: clean shutdown")
	return nil
}

// healthzHandler reports liveness. Always 200 OK with body "ok\n" once the process
// is past startup. Liveness only — readiness check (DB, NATS) lives at /readyz
// (added in Plan 02 alongside the real config and observability stack).
func healthzHandler(w http.ResponseWriter, _ *http.Request) {
	w.Header().Set("Content-Type", "text/plain; charset=utf-8")
	w.WriteHeader(http.StatusOK)
	_, _ = fmt.Fprintln(w, "ok")
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./cmd/api/... -v`
Expected:
```
=== RUN   TestHealthzReturnsOK
=== PAUSE TestHealthzReturnsOK
=== RUN   TestHealthzReturnsContentTypePlain
=== PAUSE TestHealthzReturnsContentTypePlain
=== CONT  TestHealthzReturnsOK
--- PASS: TestHealthzReturnsOK (0.00s)
=== CONT  TestHealthzReturnsContentTypePlain
--- PASS: TestHealthzReturnsContentTypePlain (0.00s)
PASS
ok      github.com/sociopulse/platform/cmd/api  ...
```

- [ ] **Step 5: Manual smoke check**

Run: `go run ./cmd/api &` then `sleep 1 && curl -s -i http://localhost:8080/healthz`
Expected:
```
HTTP/1.1 200 OK
Content-Type: text/plain; charset=utf-8
...
ok
```

Stop with `kill %1` or Ctrl-C if foreground.

- [ ] **Step 6: Commit**

```bash
git add cmd/api/main.go cmd/api/main_test.go
git commit -m "feat(api): add cmd/api skeleton with /healthz handler"
```

---

## Task 7: Add Makefile

**Files:**
- Create: `Makefile`

- [ ] **Step 1: Write Makefile**

```makefile
# СоциоПульс — top-level Makefile
# All commands run from the repo root.

SHELL := /usr/bin/env bash
.SHELLFLAGS := -eu -o pipefail -c
.DEFAULT_GOAL := help

# Where to put built binaries
BIN_DIR := bin

# Go-specific
GO         ?= go
GOFLAGS    ?=
LDFLAGS    ?= -s -w
PKG        := github.com/sociopulse/platform
COMMANDS   := api worker migrator telephony-bridge recording-uploader

# Tooling pin (override locally if needed)
GOLANGCI_LINT_VERSION ?= v1.59.1

# ----- targets -----

.PHONY: help
help: ## Show this help
	@awk 'BEGIN {FS = ":.*##"; printf "\nUsage:\n  make \033[36m<target>\033[0m\n\nTargets:\n"} /^[a-zA-Z_-]+:.*?##/ { printf "  \033[36m%-22s\033[0m %s\n", $$1, $$2 }' $(MAKEFILE_LIST)

.PHONY: tools
tools: ## Install development tools (golangci-lint)
	@echo "Installing golangci-lint $(GOLANGCI_LINT_VERSION)…"
	$(GO) install github.com/golangci/golangci-lint/cmd/golangci-lint@$(GOLANGCI_LINT_VERSION)

.PHONY: lint
lint: ## Run golangci-lint on all Go code
	golangci-lint run ./...

.PHONY: vet
vet: ## Run go vet
	$(GO) vet ./...

.PHONY: test
test: ## Run all Go tests
	$(GO) test -race -count=1 ./...

.PHONY: test-cover
test-cover: ## Run tests with coverage report
	$(GO) test -race -count=1 -coverprofile=coverage.txt -covermode=atomic ./...
	$(GO) tool cover -html=coverage.txt -o coverage.html
	@echo "Coverage report: coverage.html"

.PHONY: build
build: $(addprefix build-, $(COMMANDS)) ## Build all binaries

build-%:
	@echo "Building $*…"
	@mkdir -p $(BIN_DIR)
	$(GO) build $(GOFLAGS) -ldflags="$(LDFLAGS)" -o $(BIN_DIR)/$* ./cmd/$*

.PHONY: run
run: ## Run cmd/api locally with development config
	HTTP_ADDR=:8080 $(GO) run ./cmd/api

.PHONY: clean
clean: ## Remove build artifacts
	rm -rf $(BIN_DIR) coverage.txt coverage.html

.PHONY: docker-build
docker-build: ## Build the cmd/api Docker image
	docker build -t sociopulse-api:dev -f Dockerfile .

.PHONY: tidy
tidy: ## Tidy go.mod
	$(GO) mod tidy

.PHONY: ci
ci: lint vet test ## What CI runs
```

- [ ] **Step 2: Verify each target**

Run:
```bash
make help            # should print help
make vet             # should pass with no output
make test            # should run cmd/api tests, pass
make build-api       # should produce bin/api
ls bin/              # should show 'api' binary
make clean           # should remove bin/
```

Expected for `make build-api`:
```
Building api…
go build -ldflags="-s -w" -o bin/api ./cmd/api
```

- [ ] **Step 3: Commit**

```bash
git add Makefile
git commit -m "chore: add Makefile with build/test/lint/run/clean targets"
```

---

## Task 8: Add Dockerfile

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`

- [ ] **Step 1: Write `.dockerignore`**

```dockerignore
.git
.github
.idea
.vscode
.DS_Store
bin/
dist/
coverage.*
*.bak
*.swp
docs/
web/node_modules/
web/dist/
tests/
SocioPulse.html
social-pulse-maket/
social-pulse-maket-handoff.zip
README.md
ARCHITECTURE.md
CONTRIBUTING.md
LICENSE
CODEOWNERS
```

- [ ] **Step 2: Write `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1.7
# Multi-stage build for cmd/api.
# Image: sociopulse-api:<tag>

ARG GO_VERSION=1.22
ARG ALPINE_VERSION=3.20

# ----- builder -----
FROM golang:${GO_VERSION}-alpine AS builder

RUN apk add --no-cache git ca-certificates tzdata

WORKDIR /src

# Cache deps separately
COPY go.mod go.sum* ./
RUN go mod download

COPY . .

ARG TARGETOS=linux
ARG TARGETARCH=amd64
ARG VERSION=dev
ARG COMMIT=unknown

RUN --mount=type=cache,target=/root/.cache/go-build \
    CGO_ENABLED=0 GOOS=${TARGETOS} GOARCH=${TARGETARCH} \
    go build \
        -trimpath \
        -ldflags="-s -w -X main.version=${VERSION} -X main.commit=${COMMIT}" \
        -o /out/api \
        ./cmd/api

# ----- runtime -----
FROM alpine:${ALPINE_VERSION} AS runtime

RUN apk add --no-cache ca-certificates tzdata && \
    addgroup -S app && adduser -S -G app app

WORKDIR /app

COPY --from=builder /out/api /app/api

USER app

EXPOSE 8080

ENV HTTP_ADDR=:8080

# Healthcheck — runtime liveness only, not readiness
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD wget --quiet --tries=1 --spider http://127.0.0.1:8080/healthz || exit 1

ENTRYPOINT ["/app/api"]
```

- [ ] **Step 3: Build the image**

Run: `make docker-build` (or `docker build -t sociopulse-api:dev .`)
Expected: build succeeds with output ending in `naming to docker.io/library/sociopulse-api:dev`.

- [ ] **Step 4: Smoke-run the image**

```bash
docker run --rm -d --name sp-api-test -p 8080:8080 sociopulse-api:dev
sleep 2
curl -s http://localhost:8080/healthz
docker stop sp-api-test
```

Expected: `ok` printed.

- [ ] **Step 5: Commit**

```bash
git add Dockerfile .dockerignore
git commit -m "build: add multi-stage Dockerfile for cmd/api"
```

---

## Task 9: Add `.golangci.yml` with depguard module-boundary rules

**Files:**
- Create: `.golangci.yml`

- [ ] **Step 1: Write the linter config**

```yaml
# golangci-lint v1.59+ config
# Run: make lint

run:
  timeout: 5m
  go: "1.22"
  tests: true

output:
  formats:
    - format: colored-line-number
  sort-results: true

linters:
  disable-all: true
  enable:
    # core correctness
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    # style
    - gofmt
    - goimports
    - misspell
    - revive
    # safety
    - gosec
    - bodyclose
    - contextcheck
    - errname
    - errorlint
    - nilerr
    - rowserrcheck
    - sqlclosecheck
    # complexity
    - gocyclo
    - gocognit
    # boundaries
    - depguard
    - importas
    # idiom
    - nakedret
    - prealloc
    - unconvert
    - unparam
    - whitespace

linters-settings:
  gocyclo:
    min-complexity: 15
  gocognit:
    min-complexity: 20
  goimports:
    local-prefixes: github.com/sociopulse/platform
  revive:
    rules:
      - name: var-naming
      - name: package-comments
      - name: indent-error-flow
      - name: unexported-return
      - name: receiver-naming

  depguard:
    rules:
      module-boundaries:
        list-mode: lax
        deny:
          # All cross-module imports must go through the api/ subpackage.
          - pkg: "github.com/sociopulse/platform/internal/auth/service"
            desc: "import from internal/auth/api instead"
          - pkg: "github.com/sociopulse/platform/internal/auth/store"
            desc: "import from internal/auth/api instead"
          - pkg: "github.com/sociopulse/platform/internal/auth/events"
            desc: "import from internal/auth/api instead"
          - pkg: "github.com/sociopulse/platform/internal/crm/service"
            desc: "import from internal/crm/api instead"
          - pkg: "github.com/sociopulse/platform/internal/crm/store"
            desc: "import from internal/crm/api instead"
          - pkg: "github.com/sociopulse/platform/internal/crm/events"
            desc: "import from internal/crm/api instead"
          - pkg: "github.com/sociopulse/platform/internal/surveys/service"
            desc: "import from internal/surveys/api instead"
          - pkg: "github.com/sociopulse/platform/internal/surveys/store"
            desc: "import from internal/surveys/api instead"
          - pkg: "github.com/sociopulse/platform/internal/surveys/events"
            desc: "import from internal/surveys/api instead"
          - pkg: "github.com/sociopulse/platform/internal/dialer/service"
            desc: "import from internal/dialer/api instead"
          - pkg: "github.com/sociopulse/platform/internal/dialer/store"
            desc: "import from internal/dialer/api instead"
          - pkg: "github.com/sociopulse/platform/internal/dialer/events"
            desc: "import from internal/dialer/api instead"
          - pkg: "github.com/sociopulse/platform/internal/realtime/service"
            desc: "import from internal/realtime/api instead"
          - pkg: "github.com/sociopulse/platform/internal/realtime/store"
            desc: "import from internal/realtime/api instead"
          - pkg: "github.com/sociopulse/platform/internal/realtime/events"
            desc: "import from internal/realtime/api instead"
          - pkg: "github.com/sociopulse/platform/internal/recording/service"
            desc: "import from internal/recording/api instead"
          - pkg: "github.com/sociopulse/platform/internal/recording/store"
            desc: "import from internal/recording/api instead"
          - pkg: "github.com/sociopulse/platform/internal/recording/events"
            desc: "import from internal/recording/api instead"
          - pkg: "github.com/sociopulse/platform/internal/analytics/service"
            desc: "import from internal/analytics/api instead"
          - pkg: "github.com/sociopulse/platform/internal/analytics/store"
            desc: "import from internal/analytics/api instead"
          - pkg: "github.com/sociopulse/platform/internal/analytics/events"
            desc: "import from internal/analytics/api instead"
          - pkg: "github.com/sociopulse/platform/internal/reports/service"
            desc: "import from internal/reports/api instead"
          - pkg: "github.com/sociopulse/platform/internal/reports/store"
            desc: "import from internal/reports/api instead"
          - pkg: "github.com/sociopulse/platform/internal/reports/events"
            desc: "import from internal/reports/api instead"
          - pkg: "github.com/sociopulse/platform/internal/billing/service"
            desc: "import from internal/billing/api instead"
          - pkg: "github.com/sociopulse/platform/internal/billing/store"
            desc: "import from internal/billing/api instead"
          - pkg: "github.com/sociopulse/platform/internal/billing/events"
            desc: "import from internal/billing/api instead"
          - pkg: "github.com/sociopulse/platform/internal/audit/service"
            desc: "import from internal/audit/api instead"
          - pkg: "github.com/sociopulse/platform/internal/audit/store"
            desc: "import from internal/audit/api instead"
          - pkg: "github.com/sociopulse/platform/internal/tenancy/service"
            desc: "import from internal/tenancy/api instead"
          - pkg: "github.com/sociopulse/platform/internal/tenancy/store"
            desc: "import from internal/tenancy/api instead"

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
        - gocognit
        - gocyclo
        - errcheck
    - path: cmd/.*/main\.go
      linters:
        - gocyclo
  max-issues-per-linter: 0
  max-same-issues: 0
```

- [ ] **Step 2: Install golangci-lint locally**

Run: `make tools`
Expected: golangci-lint installs into `$(go env GOPATH)/bin/`. Verify with `which golangci-lint`.

If your `PATH` doesn't include `$GOPATH/bin`, add this to your shell profile:
```bash
export PATH="$(go env GOPATH)/bin:$PATH"
```

- [ ] **Step 3: Run lint**

Run: `make lint`
Expected: `0 issues.` (or no output, just exit 0).

If lint fails because golangci-lint binary not on PATH, run with explicit path: `$(go env GOPATH)/bin/golangci-lint run ./...`.

- [ ] **Step 4: Commit**

```bash
git add .golangci.yml
git commit -m "chore: add golangci-lint config with depguard module boundaries"
```

---

## Task 10: Add `pre-commit` hooks

**Files:**
- Create: `.pre-commit-config.yaml`

- [ ] **Step 1: Write `.pre-commit-config.yaml`**

```yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=512']
      - id: check-merge-conflict
      - id: detect-private-key
      - id: mixed-line-ending
        args: ['--fix=lf']

  - repo: https://github.com/golangci/golangci-lint
    rev: v1.59.1
    hooks:
      - id: golangci-lint
        args: ['--timeout', '5m']

  - repo: https://github.com/dnephin/pre-commit-golang
    rev: v0.5.1
    hooks:
      - id: go-fmt
      - id: go-mod-tidy

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
```

- [ ] **Step 2: Document setup in CONTRIBUTING.md**

Append to `CONTRIBUTING.md`:

```markdown

## Pre-commit hooks

Optional but recommended:

```bash
pip install pre-commit
pre-commit install
```

After install, every `git commit` runs lint, secret-scan, formatters.
```

- [ ] **Step 3: Verify (only if pre-commit is installed)**

If `pre-commit` is on system: run `pre-commit run --all-files`.
If not, skip this step — it's an opt-in dev tool.

- [ ] **Step 4: Commit**

```bash
git add .pre-commit-config.yaml CONTRIBUTING.md
git commit -m "chore: add pre-commit hooks config"
```

---

## Task 11: Add CI workflow (`.github/workflows/ci.yml`)

**Files:**
- Create: `.github/workflows/ci.yml`
- Create: `.github/workflows/codeql.yml`

- [ ] **Step 1: Write CI workflow**

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.22"
  GOLANGCI_LINT_VERSION: v1.59.1

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: ${{ env.GOLANGCI_LINT_VERSION }}
          args: --timeout 5m

  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Run tests
        run: make test
      - name: Run tests with coverage
        run: make test-cover
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ github.run_id }}
          path: coverage.html
          retention-days: 7

  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: Build all binaries
        run: make build
      - name: Verify binaries exist
        run: ls -la bin/

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build cmd/api image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: sociopulse-api:ci-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  vuln:
    name: Vulnerability scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - name: Run govulncheck
        run: govulncheck ./...
      - name: Run osv-scanner
        uses: google/osv-scanner-action/osv-scanner-action@v1.7.4
        with:
          scan-args: |-
            --recursive
            --skip-git
            ./

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

- [ ] **Step 2: Write CodeQL workflow**

Create `.github/workflows/codeql.yml`:

```yaml
name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 3 * * 1"   # Mondays 03:00 UTC

jobs:
  analyze:
    name: Analyze (Go)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [go]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      - uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/autobuild@v3
      - uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
```

- [ ] **Step 3: Verify YAML syntax**

If `yamllint` is on the system: `yamllint .github/workflows/`.
Otherwise check by eye — proper indents, no tabs.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/
git commit -m "ci: add lint/test/build/docker/vuln/secret-scan + CodeQL workflows"
```

---

## Task 12: Sanity-test the full toolchain end-to-end

**Files:** none

- [ ] **Step 1: Clean state**

Run: `make clean && go clean -testcache`
Expected: removes `bin/`, `coverage.*`, clears Go test cache.

- [ ] **Step 2: Full pipeline**

Run: `make ci`
Expected: runs `lint vet test`, all pass.

```
golangci-lint run ./...
0 issues.
go vet ./...
go test -race -count=1 ./...
ok      github.com/sociopulse/platform/cmd/api  ...
```

- [ ] **Step 3: Build all binaries**

Run: `make build && ls bin/`
Expected: prints all 5 binaries: `api`, `migrator`, `recording-uploader`, `telephony-bridge`, `worker`.

(Currently only `api` has real code; the others are stubs from `package main` files we'll add as a final defensive step in Task 13.)

- [ ] **Step 4: Run cmd/api and curl /healthz**

```bash
make run &
APIPID=$!
sleep 1
curl -s -i http://localhost:8080/healthz
kill $APIPID
```

Expected: `200 OK` with body `ok`.

- [ ] **Step 5: Build Docker image and smoke-test**

```bash
make docker-build
docker run --rm -d --name sp-api-smoke -p 8081:8080 sociopulse-api:dev
sleep 2
curl -s http://localhost:8081/healthz
docker stop sp-api-smoke
```

Expected: `ok`.

- [ ] **Step 6: No commit — this is verification only.**

If anything fails — fix and re-run before proceeding.

---

## Task 13: Add stub `main.go` for sibling cmd binaries (so `make build` works)

The other four `cmd/<bin>/main.go` files don't exist yet, so `make build` fails on them. Add minimal stubs that fail with a clear message until their real implementation lands.

**Files:**
- Create: `cmd/worker/main.go`
- Create: `cmd/migrator/main.go`
- Create: `cmd/telephony-bridge/main.go`
- Create: `cmd/recording-uploader/main.go`

- [ ] **Step 1: Add `cmd/worker/main.go`**

```go
// Package main is the entrypoint for cmd/worker.
//
// Implementation lands in subsequent plans (jobs queue + leader election).
// This stub allows `make build` to succeed and provides a clear runtime error.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "cmd/worker: not yet implemented; see docs/superpowers/plans/")
	os.Exit(64)
}
```

- [ ] **Step 2: Add `cmd/migrator/main.go`** (same pattern)

```go
// Package main is the entrypoint for cmd/migrator.
//
// Real implementation in Plan 03 (Database & migrations).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "cmd/migrator: not yet implemented; see Plan 03")
	os.Exit(64)
}
```

- [ ] **Step 3: Add `cmd/telephony-bridge/main.go`** (same pattern)

```go
// Package main is the entrypoint for cmd/telephony-bridge.
//
// Real implementation in Plan 09.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "cmd/telephony-bridge: not yet implemented; see Plan 09")
	os.Exit(64)
}
```

- [ ] **Step 4: Add `cmd/recording-uploader/main.go`** (same pattern)

```go
// Package main is the entrypoint for cmd/recording-uploader.
//
// Real implementation in Plan 12.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "cmd/recording-uploader: not yet implemented; see Plan 12")
	os.Exit(64)
}
```

- [ ] **Step 5: Verify build**

Run: `make clean && make build && ls bin/`
Expected: 5 binaries: `api migrator recording-uploader telephony-bridge worker`.

- [ ] **Step 6: Commit**

```bash
git add cmd/worker/main.go cmd/migrator/main.go cmd/telephony-bridge/main.go cmd/recording-uploader/main.go
git commit -m "feat: add stub main.go for worker/migrator/telephony-bridge/recording-uploader"
```

---

## Task 14: Documentation polish — add doc-tree index

**Files:**
- Create: `docs/README.md`

- [ ] **Step 1: Write doc index**

```markdown
# СоциоПульс — Documentation

## Top-level

- [README](../README.md) — project intro, quickstart
- [ARCHITECTURE](../ARCHITECTURE.md) — short architecture overview
- [CONTRIBUTING](../CONTRIBUTING.md) — dev workflow, commit style, module boundaries
- [LICENSE](../LICENSE) — proprietary

## Specifications

- [docs/superpowers/specs/2026-05-06-sociopulse-system-design.md](superpowers/specs/2026-05-06-sociopulse-system-design.md)
  — full system design doc (FR/NFR, architecture, ADRs).

## Implementation plans

Sequenced plans for agentic execution. Each plan produces working, committable software.

- [00 — Foundation](superpowers/plans/2026-05-06-00-foundation.md) — repo bootstrap, lint, CI, hello-world cmd/api **(this plan)**
- 01 — Infrastructure (Terraform + k8s)
- 02 — cmd/api skeleton (config, observability, gateway middleware)
- 03 — Database & migrations
- 04 — Tenancy module
- 05 — Auth module
- 06 — CRM module
- 07 — Surveys module
- 08 — FreeSWITCH cluster + recording-uploader
- 09 — telephony-bridge
- 10 — Dialer module
- 11 — Realtime module
- 12 — Recording module
- 13 — Analytics + Reports
- 14 — Billing module
- 15 — Frontend foundation
- 16 — Frontend operator pages
- 17 — Frontend admin pages 1
- 18 — Frontend admin survey builder
- 19 — Frontend admin pages 2

## Architecture

- [docs/architecture/](architecture/) — ADR copies, diagrams, deep-dive notes

## Runbooks

- [docs/runbooks/](runbooks/) — ops procedures (deploy, incident, DR)

## API documentation

- [docs/api/](api/) — OpenAPI specs, gRPC `.proto` files, generated reference
```

- [ ] **Step 2: Commit**

```bash
git add docs/README.md
git commit -m "docs: add docs/README.md as documentation index"
```

---

## Task 15: Final verification — repo should be in clean buildable state

**Files:** none

- [ ] **Step 1: Status check**

Run: `git status`
Expected: `nothing to commit, working tree clean`.

- [ ] **Step 2: Log check**

Run: `git log --oneline`
Expected: ~14 commits, all conventional (feat:, chore:, docs:, ci:, build:).

- [ ] **Step 3: Run full CI locally**

Run: `make ci && make build`
Expected: lint, vet, test, build all pass; `bin/` contains 5 binaries.

- [ ] **Step 4: Tree summary**

Run: `find . -maxdepth 2 -type d -not -path './.git*' -not -path './social-pulse-maket*' -not -path './bin' | sort`
Expected:
```
.
./.github
./.github/workflows
./cmd
./cmd/api
./cmd/migrator
./cmd/recording-uploader
./cmd/telephony-bridge
./cmd/worker
./configs
./configs/development
./deployments
./docs
./docs/api
./docs/architecture
./docs/runbooks
./docs/superpowers
./internal
./internal/analytics
./internal/audit
./internal/auth
./internal/billing
./internal/crm
./internal/dialer
./internal/gateway
./internal/realtime
./internal/recording
./internal/reports
./internal/surveys
./internal/tenancy
./migrations
./pkg
./tests
./tests/e2e
./tests/load
./web
```

- [ ] **Step 5: Tag this milestone**

```bash
git tag -a v0.0.1-foundation -m "Plan 00 complete: repo skeleton with cmd/api /healthz"
git tag -l
```

Expected: `v0.0.1-foundation` listed.

This isn't pushed anywhere yet — purely local marker.

- [ ] **Step 6: No commit. Plan 00 done.**

---

## Verification summary

After completing all 15 tasks, the repo state is:

- Git repo initialized, ~14 commits, 1 tag.
- Go module `github.com/sociopulse/platform` builds cleanly.
- `make help/lint/vet/test/build/run/clean/ci/docker-build/tidy/tools` all work.
- `cmd/api` binary serves `GET /healthz → 200 ok`.
- All 5 cmd/-binaries build (4 of them are stubs that fail with clear error).
- 12 internal modules have placeholder `<module>/api/.gitkeep`.
- `.golangci.yml` enforces module boundaries via depguard.
- Pre-commit hooks defined in `.pre-commit-config.yaml`.
- GitHub Actions workflows: `ci.yml` (lint/test/build/docker/vuln/secret-scan), `codeql.yml`.
- LICENSE, README, ARCHITECTURE, CONTRIBUTING, CODEOWNERS in place.
- Documentation tree under `docs/{architecture,runbooks,api,superpowers}/`.

This unlocks all subsequent plans:
- **Plan 01** (Infrastructure) populates `deployments/` with Terraform + k8s manifests.
- **Plan 02** (cmd/api skeleton) extends `cmd/api` with config loader, observability, middleware.
- **Plan 03+** (modules) populates `internal/<module>/{api,service,store,events}/` with real Go code.

---

## Self-review

**Spec coverage check (against spec §4.2, §5.1–5.5, §17 baseline):**
- §4.2 deploy artifacts (cmd/api, cmd/worker, cmd/migrator, cmd/telephony-bridge, cmd/recording-uploader) → all five `cmd/` directories created with main.go (stub or real). ✅
- §5.1 modular monolith principles (one Go module, one binary, depguard) → go.mod + .golangci.yml. ✅
- §5.2 module catalog (12 modules) → 12 `internal/<module>/api/` directories. ✅
- §5.5 dependency graph: no cycles → enforced by depguard rules. ✅
- §17 test pyramid baseline: unit-test stack ready (Go testing + race) → cmd/api/main_test.go exists, `make test` runs with `-race`. ✅

**Placeholder scan:** all "implement later" stubs have explicit error message + plan-reference; no TBD/TODO. ✅

**Type consistency:** `healthzHandler` in main.go and main_test.go match. ✅

**Out-of-scope (correctly deferred to later plans):**
- Real config loading (Plan 02)
- Observability stack (Plan 02)
- Database setup (Plan 03)
- Application logic (Plans 04+)

Plan 00 verified.

---

**Plan complete and saved to `docs/superpowers/plans/2026-05-06-00-foundation.md`. After this plan executes, the repo is in green-field but production-grade buildable state — every commit lands tested, linted, and CI-validated.**

I will continue producing the remaining 19 plans in the same level of detail. Watch for `2026-05-06-01-infrastructure.md` next.
